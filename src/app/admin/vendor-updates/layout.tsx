import type { Metadata } from "next";

export const metadata: Metadata = { title: "تحديثات مقدمي الخدمات" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
