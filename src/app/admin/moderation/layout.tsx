import type { Metadata } from "next";

export const metadata: Metadata = { title: "المراجعة والإشراف" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
