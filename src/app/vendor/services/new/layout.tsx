import type { Metadata } from "next";

export const metadata: Metadata = { title: "إضافة خدمة جديدة" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
